# URGENT — ACK + blocker check (Q3_v2)

Specialist,
SC requires immediate status confirmation.

Reply by producing one small file in outbox within 10 minutes:
- `outbox/Q3_V2_CP0_ACK_2026-03-08.md`

Content format (single line):
`STATUS | STARTED/NOT_STARTED | blocker_cause(if any) | missing_input(if any) | CP1_ETA`

Important:
- If NOT_STARTED, explicitly state why task was not picked up.
- If blocked, use blocker format exactly.
- Then start Q3_v2 execution immediately after ACK.
