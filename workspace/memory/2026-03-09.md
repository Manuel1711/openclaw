# 2026-03-09

- Git migration completed: user requested a single monorepo in `/home/manuel/.openclaw` instead of separate nested repo for `workspace/`.
- `workspace/.git` was removed from active use and backed up as `workspace/.git.backup-<timestamp>` before migration.
- `.openclaw` repo now includes `workspace/` contents and pushes to `git@github.com:Manuel1711/openclaw.git` on branch `main`.
- Added automation script: `/home/manuel/.openclaw/scripts/push-openclaw.sh` (add/commit/push + basic secret pre-check).
- Security/versioning policy clarified for config: keep `openclaw.json` ignored (contains secrets), track `openclaw.redacted.json` for reproducible config debugging.
- Confirmed operational incident root cause in logs: failures were primarily due to API credit exhaustion (`402 requires more credits`) on OpenRouter GPT-5.3-Codex requests, not only generic rate-limit 429.
- Discord reliability intervention applied:
  - Disabled mention-gating at guild level for `1479524789327761582` (`requireMention: false`).
  - Pinned explicit `#general` channel policy (`1479524789994524897`): `enabled=true`, `allow=true`, `requireMention=false`, `ignoreOtherMentions=false`.
  - Performed controlled gateway restarts and verified `Discord: ok` via `openclaw doctor --non-interactive`.
  - Mitigated latency by resetting bloated Discord channel sessions (`agent:main:discord:channel:1479524789994524897` and derived fork), with backup of session store created before edit.
  - Residual risk identified: intermittent Discord websocket close/reconnect events (code 1005), likely transport/network-path instability rather than policy misconfiguration.

## ERC8004 dApp lessons captured
- Root cause of “all scores are 0”: snapshot generated from partial backfill; ranking must be considered valid only after lag=0.
- Root cause of “mobile broken”: agents table attempted full render of ~28k rows; fixed with pagination/load-more and mobile horizontal scroll.
- Root cause of “not updating over time”: scheduled workflow red; fixed by adding fallback public RPC when `ETH_RPC_URL` secret is missing.
- Prevention rules adopted:
  1) Always validate `/` and `/index.html` after deploy.
  2) Always check latest workflow status before claiming live updates.
  3) For large datasets, never ship unpaginated tables.
  4) Keep an explicit incident checklist in project README.

## Codex consumption incident + prevention
- Problem observed: codex allowance consumed faster than expected.
- Decision: keep `openai-codex` as fixed model (plan-aligned), optimize call-efficiency instead of model switching.
- Added policy file: `CODEX_USAGE_POLICY.md`.
- Core controls now documented: batching, dedup, one-retry cap with backoff, no retry loops, prompt minimization, and alerts at 30%/15% remaining.

